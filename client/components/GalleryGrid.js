import React from 'react';

const GalleryGrid = React.createClass({
  render() {
    return (
      <div className="gallery-grid">
        This is a gallery grid
      </div>
    )
  }
});

export default GalleryGrid;
